import { UxBehaviorDirective } from './ux-behavior.directive';

describe('UxBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new UxBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
